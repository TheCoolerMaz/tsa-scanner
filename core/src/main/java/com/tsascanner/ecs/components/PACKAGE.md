# ECS Components

This package contains all the component classes used in the Entity Component System (ECS). Components are data containers that hold information about an entity, such as its position, velocity, or visual representation.
